# Versioning — DHG Core 4

DHG Core uses Semantic Versioning:

**MAJOR.MINOR.PATCH**

- MAJOR — breaking changes
- MINOR — new features
- PATCH — fixes

Example:

```
v0.1.0 — first vertical slice
v0.2.0 — multi-tenancy + jobs
```
